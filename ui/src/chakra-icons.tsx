import React from 'react'

export const ExternalLinkIcon = (props: any) => <span style={{ marginLeft: 4, ...(props?.style || {}) }}>?</span>
export const DownloadIcon = (props: any) => <span style={{ marginLeft: 4, ...(props?.style || {}) }}>?</span>
